// language switching will be managed through here

import {createSlice} from '@reduxjs/toolkit';

const lanSlice = createSlice({
  name: 'lan',

  initialState: {
    dummy: null
  },

  reducers: {

  }
});

export default lanSlice.reducer;
